# Test and lint dependencies for the Dockhand integration.
#
# Requires Python 3.14.2+ and the matching pytest-homeassistant-custom-component
# version (which pins an exact homeassistant release).
#
# PHCC version ↔ HA version mapping (patch increments daily with HA releases):
#   0.13.317+ requires Python >=3.14
#   0.13.338  pins homeassistant==2026.6.2  (use this or newer for 2026.3+ targets)
#
# To run the full suite locally (see docs/development.md for venv setup):
#   pytest tests/
#
# CI runs on Python 3.14 (latest patch) via .github/workflows/ci.yml

pytest-homeassistant-custom-component==0.13.339
ruff
